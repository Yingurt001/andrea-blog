# 中文科研博客写作偏好

本仓库中的中文科研文章，默认参考 `src/content/posts/continuous-latent-language-models-comparison.md` 前半部分的作者改写风格。

## 语气

- 使用直接、解释型、有个人判断的科研讨论语气。
- 热情来自问题本身、机制分析和明确判断，不靠段子、口号或刻意制造“知乎感”。
- 可以使用第一人称，也可以保留 Encoder、Denoiser、Decoder、latent、Flow Matching 等英文术语。
- 不要为了显得口语化而降低技术精度；优先说明训练目标、压力放在哪个模块、为什么这样设计以及代价是什么。

## 结构

- 标题和小标题直接描述问题或方法，例如“谁负责让 latent 变得好生成？”“VAE 和 DiT 进行联合训练”。
- 优先按具体问题、机制、trade-off 和实验现象展开。
- 比较模型时尽量放进统一的 Encoder–Denoiser–Decoder 框架，但不要为了结构整齐重复同一句结论。
- 明确区分代码中已经实现的内容、现有实验支持的判断和仍待验证的方案。

## 避免的表达

- 不使用健身、减肥、恋爱、开车、接球、搬石头、签合同、盖房子等与技术机制无关的比喻。
- 避免类似“latent 不减肥，给 denoiser 办健身卡”“VAE 和 DiT 一起过日子”“Decoder 学会接一种球”这类标题或句子。
- 避免把模块过度拟人化，例如“谁偷懒”“把锅甩给谁”“谁都别抄答案”。如需说明 shortcut，直接写出模型如何绕过训练约束。
- 避免空泛的强结论，例如“真正的核心”“最关键的洞见”“重新定义了整个领域”。结论后面应紧跟机制或证据。
- 不要机械增加粗体、三段式口号或整齐的反问句。

## 编辑原则

- 用户亲自改写的段落是最高优先级的风格样本，不要擅自改回更工整的 AI 文风。
- 不刻意模仿用户草稿中的输入法错误或标点疏漏，但保留其句子节奏、术语习惯和解释顺序。
- 在不改变技术含义的前提下清理明显的 AI 腔；如果“活泼”和“准确”冲突，选择准确。
